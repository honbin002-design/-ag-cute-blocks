import * as THREE from 'three';
const VERSION='V0.5.45',IDS=new Set(['special2','special5']),FALLBACK=new Set(['fish','farm','chop','attack','dodge']);
function findBone(root,patterns){let hit=null;root?.traverse?.(o=>{if(hit||!o.isBone)return;const n=(o.name||'').toLowerCase();if(patterns.some(p=>n.includes(p)))hit=o});return hit}
function install(){const rt=globalThis.__AGCB_TEST_CHARACTER_RUNTIME;if(!rt||rt.__actionsV545)return false;const base=rt.create.bind(rt);rt.create=async id=>{const c=await base(id);if(!IDS.has(id)||!c?.visual)return c;const basePlay=c.play?.bind(c)||(()=>{}),baseUpdate=c.update?.bind(c)||(()=>{}),bones={spine:findBone(c.visual,['spine','chest']),lArm:findBone(c.visual,['leftarm','l_upperarm','upperarm_l','mixamorigleftarm']),rArm:findBone(c.visual,['rightarm','r_upperarm','upperarm_r','mixamorigrightarm']),lFore:findBone(c.visual,['leftforearm','l_lowerarm','lowerarm_l','mixamorigleftforearm']),rFore:findBone(c.visual,['rightforearm','r_lowerarm','lowerarm_r','mixamorigrightforearm']),lLeg:findBone(c.visual,['leftupleg','l_thigh','thigh_l','mixamorigleftupleg']),rLeg:findBone(c.visual,['rightupleg','r_thigh','thigh_r','mixamorigrightupleg'])};const rest={};for(const[k,b]of Object.entries(bones))if(b)rest[k]=b.quaternion.clone();const q=new THREE.Quaternion(),target=new THREE.Quaternion();let action='idle',phase=0,activeFallback=false;
function pose(k,x=0,y=0,z=0,a=.28){const b=bones[k],r=rest[k];if(!b||!r)return;q.setFromEuler(new THREE.Euler(x,y,z));target.copy(r).multiply(q);b.quaternion.slerp(target,a)}
function reset(a=.18){for(const[k,b]of Object.entries(bones))if(b&&rest[k])b.quaternion.slerp(rest[k],a)}
function fallbackUpdate(dt){phase+=dt;reset(.22);if(action==='fish'){pose('lArm',-.46,0,-.45);pose('rArm',-.46,0,.45);pose('lFore',-.72,0,-.08);pose('rFore',-.72,0,.08);pose('spine',.08,0,Math.sin(phase*2.1)*.02,.18)}else if(action==='farm'){pose('spine',.38,0,0,.3);pose('lArm',-.58,0,-.42,.32);pose('rArm',-.58,0,.42,.32);pose('lFore',-.48,0,0,.3);pose('rFore',-.48,0,0,.3)}else if(action==='chop'){const s=Math.sin(phase*7.5);pose('spine',.14,0,s*.08,.28);pose('lArm',-.86+s*.32,0,-.48,.35);pose('rArm',-.86+s*.32,0,.48,.35);pose('lFore',-.34,0,0,.3);pose('rFore',-.34,0,0,.3)}else if(action==='attack'){const s=Math.sin(phase*10);pose('spine',.08,0,s*.12,.32);pose('rArm',-.22+s*.72,0,.35,.38);pose('rFore',-.32,0,0,.34);pose('lArm',.12,0,-.22,.25)}else if(action==='dodge'){pose('spine',.28,0,.25,.34);pose('lLeg',-.30,0,0,.32);pose('rLeg',.30,0,0,.32);pose('lArm',.18,0,-.28,.28);pose('rArm',-.12,0,.28,.28)}}
c.play=name=>{action=name||'idle';const hasEmbedded=!!c.actions?.[action];activeFallback=FALLBACK.has(action)&&!hasEmbedded;basePlay(action)};c.update=dt=>{baseUpdate(dt);if(activeFallback)fallbackUpdate(dt)};c.available=[...new Set([...(c.available||[]),...FALLBACK])];c.root.userData.agInteractionFallback={version:VERSION,bonesFound:Object.values(bones).filter(Boolean).length};return c};rt.__actionsV545=true;globalThis.__AGCB_SPECIAL_ACTIONS={version:VERSION};return true}
let tries=0;const timer=setInterval(()=>{if(install()||++tries>100)clearInterval(timer)},40);install();