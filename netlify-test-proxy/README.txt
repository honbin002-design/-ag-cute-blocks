AG Cute Blocks TEST proxy publish directory.
Runtime content is reverse-proxied from an immutable dev-v0.1 GitHub snapshot through jsDelivr, while keeping a distinct Netlify origin for TEST browser storage isolation.
Never point this TEST proxy at the GitHub Pages PROD origin. Update the pinned commit only after TEST source validation.
