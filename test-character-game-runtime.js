import * as THREE from 'three';
import {GLTFLoader} from 'https://cdn.jsdelivr.net/npm/three@0.180.0/examples/jsm/loaders/GLTFLoader.js';
import {clone as cloneSkeleton} from 'https://cdn.jsdelivr.net/npm/three@0.180.0/examples/jsm/utils/SkeletonUtils.js';

const loader=new GLTFLoader();const MODEL_TIMEOUT=20000,ANIMATION_TIMEOUT=9000,cache=new Map();
const TEST_CHARACTERS={test1:{label:'測試角色1',model:'https://raw.githubusercontent.com/eturner58/game-assets/main/kenney/3D%20assets/Blocky%20Characters/Models/GLB%20format/character-a.glb'},test2:{label:'測試角色2',model:'https://raw.githubusercontent.com/Seyamalam/blood-league-kickoff/main/public/assets/vendor/quaternius/night-striker.glb'},test3:{label:'測試角色3',model:'https://raw.githubusercontent.com/iamenahs/xlunar-ai-avatar/main/public/avatars/VRoid_Sample_A.glb',style:'CC0 VRoid日系女角・白髮貓耳目標V5',animeBase:true}};
function load(url,timeout=MODEL_TIMEOUT){if(cache.has(url))return cache.get(url);const task=new Promise((resolve,reject)=>{let done=false;const timer=setTimeout(()=>{if(!done){done=true;reject(new Error(`載入逾時：${url.split('/').pop()}`))}},timeout);loader.load(url,g=>{if(!done){done=true;clearTimeout(timer);resolve(g)}},undefined,e=>{if(!done){done=true;clearTimeout(timer);reject(e)}})});cache.set(url,task);task.catch(()=>cache.delete(url));return task}
function normalize(root,targetHeight=2){root.updateMatrixWorld(true);const box=new THREE.Box3().setFromObject(root),size=box.getSize(new THREE.Vector3()),scale=targetHeight/Math.max(size.y,.001);root.scale.multiplyScalar(scale);root.updateMatrixWorld(true);const b=new THREE.Box3().setFromObject(root),c=b.getCenter(new THREE.Vector3());root.position.x-=c.x;root.position.z-=c.z;root.position.y-=b.min.y;root.updateMatrixWorld(true);return root}
function chooseClip(clips,keys){for(const k of keys){const c=clips.find(x=>(x.name||'').toLowerCase().includes(k));if(c)return c}return null}
function recolorMaterial(mat,color){if(!mat)return mat;const n=mat.clone();if(n.color)n.color.set(color);if('roughness'in n)n.roughness=Math.max(n.roughness??.5,.48);return n}
function tintAnimeHair(root){root.traverse(o=>{if(!o.isMesh&&!o.isSkinnedMesh)return;const names=`${o.name||''} ${Array.isArray(o.material)?o.material.map(m=>m?.name||'').join(' '):o.material?.name||''}`.toLowerCase();if(!/(hair|髮|kami)/.test(names))return;const arr=Array.isArray(o.material)?o.material:[o.material],next=arr.map(m=>recolorMaterial(m,0xf1f3ff));o.material=Array.isArray(o.material)?next:next[0]})}
function findBone(root,names){for(const n of names){const o=root.getObjectByName(n);if(o)return o}return null}
function addMockupStyle(root){
 const navy=new THREE.MeshStandardMaterial({color:0x152b5c,roughness:.62,metalness:.02}),inner=new THREE.MeshStandardMaterial({color:0xf0e9ff,roughness:.7}),gold=new THREE.MeshStandardMaterial({color:0xf3ca55,roughness:.45,metalness:.1});
 const head=findBone(root,['J_Bip_C_Head','Normalized_J_Bip_C_Head']),chest=findBone(root,['J_Bip_C_UpperChest','Normalized_J_Bip_C_UpperChest','J_Bip_C_Chest','Normalized_J_Bip_C_Chest']);
 if(head){
  const earRig=new THREE.Group();earRig.name='ag-cat-ear-rig';head.add(earRig);earRig.position.set(0,.12,0);
  const earGeo=new THREE.ConeGeometry(.072,.19,3),innerGeo=new THREE.ConeGeometry(.038,.105,3);
  const left=new THREE.Mesh(earGeo,navy),right=new THREE.Mesh(earGeo,navy),il=new THREE.Mesh(innerGeo,inner),ir=new THREE.Mesh(innerGeo,inner);
  left.position.set(-.092,.09,0);right.position.set(.092,.09,0);il.position.set(-.092,.09,-.038);ir.position.set(.092,.09,-.038);
  left.rotation.z=.07;right.rotation.z=-.07;il.rotation.z=.07;ir.rotation.z=-.07;left.name='ag-cat-ear-left';right.name='ag-cat-ear-right';earRig.add(left,right,il,ir);
 }
 if(chest){
  const bow=new THREE.Group();bow.name='ag-back-bow';chest.add(bow);bow.position.set(0,.015,.115);bow.rotation.set(0,0,0);
  const wingGeo=new THREE.SphereGeometry(.075,12,8),l=new THREE.Mesh(wingGeo,navy),r=new THREE.Mesh(wingGeo,navy);l.scale.set(1.45,.62,.34);r.scale.set(1.45,.62,.34);l.position.x=-.088;r.position.x=.088;
  const knot=new THREE.Mesh(new THREE.SphereGeometry(.038,10,8),navy),star=new THREE.Mesh(new THREE.OctahedronGeometry(.026,0),gold);star.position.z=-.034;bow.add(l,r,knot,star);
 }
 root.userData.agAccessories={anchored:true,space:'bone-local',earScale:'medium-v3',bowScale:'medium-v3'};
}
const VRoidAliases={hips:['J_Bip_C_Hips','Normalized_J_Bip_C_Hips'],spine:['J_Bip_C_Spine','Normalized_J_Bip_C_Spine'],chest:['J_Bip_C_Chest','Normalized_J_Bip_C_Chest'],lUpperArm:['J_Bip_L_UpperArm','Normalized_J_Bip_L_UpperArm'],rUpperArm:['J_Bip_R_UpperArm','Normalized_J_Bip_R_UpperArm'],lLowerArm:['J_Bip_L_LowerArm','Normalized_J_Bip_L_LowerArm'],rLowerArm:['J_Bip_R_LowerArm','Normalized_J_Bip_R_LowerArm'],lUpperLeg:['J_Bip_L_UpperLeg','Normalized_J_Bip_L_UpperLeg'],rUpperLeg:['J_Bip_R_UpperLeg','Normalized_J_Bip_R_UpperLeg'],lLowerLeg:['J_Bip_L_LowerLeg','Normalized_J_Bip_L_LowerLeg'],rLowerLeg:['J_Bip_R_LowerLeg','Normalized_J_Bip_R_LowerLeg']};
function makeProceduralAnimator(root){const bones={};for(const[k,n]of Object.entries(VRoidAliases))bones[k]=findBone(root,n);const rest={};for(const[k,b]of Object.entries(bones))if(b)rest[k]=b.quaternion.clone();const poseQ=new THREE.Quaternion(),targetQ=new THREE.Quaternion();let action='idle',phase=0;function pose(k,x=0,y=0,z=0,blend=.24){const b=bones[k],r=rest[k];if(!b||!r)return;poseQ.setFromEuler(new THREE.Euler(x,y,z,'XYZ'));targetQ.copy(r).multiply(poseQ);b.quaternion.slerp(targetQ,blend)}function reset(blend=.18){for(const[k,b]of Object.entries(bones))if(b&&rest[k])b.quaternion.slerp(rest[k],blend)}function update(dt){phase+=dt;const s=Math.sin(phase*(action==='run'?10:action==='walk'?7:2.1)),c=Math.cos(phase*(action==='run'?10:action==='walk'?7:2.1));reset(.15);if(action==='idle'){pose('lUpperArm',.02,0,1.18,.2);pose('rUpperArm',-.02,0,-1.18,.2);pose('lLowerArm',0,0,.08,.2);pose('rLowerArm',0,0,-.08,.2);pose('chest',.015*Math.sin(phase*2),0,.018*Math.sin(phase),.12);return}if(action==='walk'||action==='run'){const run=action==='run',legAmp=run?.72:.48,armAmp=run?.58:.38;pose('lUpperArm',s*armAmp,0,1.08,.32);pose('rUpperArm',-s*armAmp,0,-1.08,.32);pose('lLowerArm',run?.28:.12,0,.10,.28);pose('rLowerArm',run?.28:.12,0,-.10,.28);pose('lUpperLeg',-s*legAmp,0,0,.35);pose('rUpperLeg',s*legAmp,0,0,.35);pose('lLowerLeg',Math.max(0,s)*-(run?.72:.42),0,0,.32);pose('rLowerLeg',Math.max(0,-s)*-(run?.72:.42),0,0,.32);pose('chest',run?.08:.04,0,c*(run?.055:.03),.2);return}if(action==='jump'){pose('lUpperArm',-.5,0,-.72,.35);pose('rUpperArm',-.5,0,.72,.35);pose('lUpperLeg',-.25,0,0,.35);pose('rUpperLeg',-.25,0,0,.35);pose('lLowerLeg',-.42,0,0,.35);pose('rLowerLeg',-.42,0,0,.35);return}if(action==='fish'){pose('lUpperArm',-.38,0,-.55,.25);pose('rUpperArm',-.38,0,.55,.25);pose('lLowerArm',-.65,0,-.1,.25);pose('rLowerArm',-.65,0,.1,.25);return}if(action==='farm'){pose('chest',.32,0,0,.25);pose('lUpperArm',-.48,0,-.45,.3);pose('rUpperArm',-.48,0,.45,.3);pose('lLowerArm',-.55,0,0,.3);pose('rLowerArm',-.55,0,0,.3);return}if(action==='chop'||action==='attack'){const hit=Math.sin(phase*8);pose('lUpperArm',-.85+hit*.35,0,-.45,.35);pose('rUpperArm',-.85+hit*.35,0,.45,.35);pose('lLowerArm',-.42,0,0,.3);pose('rLowerArm',-.42,0,0,.3);return}if(action==='dodge'){pose('chest',.25,0,.22,.32);pose('lUpperLeg',-.22,0,0,.3);pose('rUpperLeg',.22,0,0,.3)}}return{available:['idle','walk','run','jump','fish','farm','chop','attack','dodge'],play(name){action=name||'idle'},update,get action(){return action},bonesFound:Object.values(bones).filter(Boolean).length}}
async function create(id){const spec=TEST_CHARACTERS[id];if(!spec)throw new Error(`Unknown test character: ${id}`);const source=await load(spec.model),root=cloneSkeleton(source.scene);let clips=source.animations||[];normalize(root);if(spec.animeBase)tintAnimeHair(root);if(spec.style)addMockupStyle(root);root.name=`ag-${id}`;root.userData={...(root.userData||{}),agTestCharacter:id,label:spec.label,style:spec.style||'',animeBase:!!spec.animeBase};let animationLibraryStatus='embedded';if(spec.animations){try{const lib=await load(spec.animations,ANIMATION_TIMEOUT);if(lib.animations?.length){clips=lib.animations;animationLibraryStatus='loaded'}else animationLibraryStatus='empty'}catch(e){animationLibraryStatus='skipped'}}const mixer=new THREE.AnimationMixer(root),actions={},map={idle:['idle','stand','breath'],walk:['walk'],run:['run','sprint','jog'],jump:['jump'],fish:['fish'],farm:['farm','plant','hoe'],chop:['chop','axe'],attack:['attack','slash','punch'],dodge:['dodge','roll']};for(const[name,keys]of Object.entries(map)){const clip=chooseClip(clips,keys);if(clip)actions[name]=mixer.clipAction(clip,root)}let current=null,procedural=null;if(spec.animeBase&&Object.keys(actions).length<3){procedural=makeProceduralAnimator(root);animationLibraryStatus=`procedural:${procedural.bonesFound}`}function play(name){if(procedural){procedural.play(name);return}const next=actions[name]||actions.idle||actions.walk||Object.values(actions)[0];if(!next||next===current)return;current?.fadeOut(.12);next.reset().fadeIn(.12).play();current=next}play('idle');return{root,mixer,actions,clips,play,update(dt){if(procedural)procedural.update(dt);else mixer.update(dt)},available:procedural?procedural.available:Object.keys(actions),animationLibraryStatus,hairStatus:spec.animeBase?'embedded':'none',styleStatus:spec.style||'',forwardYaw:Math.PI,procedural:!!procedural}}
globalThis.__AGCB_TEST_CHARACTER_RUNTIME={version:13,characters:TEST_CHARACTERS,create,modelTimeout:MODEL_TIMEOUT,animationTimeout:ANIMATION_TIMEOUT,cache,sharedThree:true,target:'cc0-vroid-anime-cat-ear-v5-bone-local-medium-accessories'};export{TEST_CHARACTERS,create};