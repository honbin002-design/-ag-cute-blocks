name: Building extension check

on:
  push:
    branches: [dev-v0.1]
  pull_request:
    branches: [main]

jobs:
  building-extension:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
      - name: Parse extension modules
        run: |
          node --input-type=module --check < building-extension-runtime.js
          node --input-type=module --check < procedural-material-runtime.js
          node --input-type=module --check < collision-cache-runtime.js
      - name: Verify stairs and ceramic persistence hooks
        shell: python
        run: |
          import pathlib,sys
          ext=pathlib.Path('building-extension-runtime.js').read_text(encoding='utf-8')
          mat=pathlib.Path('procedural-material-runtime.js').read_text(encoding='utf-8')
          collision=pathlib.Path('collision-cache-runtime.js').read_text(encoding='utf-8')
          boot=pathlib.Path('bootstrap-v045.js').read_text(encoding='utf-8')
          sw=pathlib.Path('sw.js').read_text(encoding='utf-8')
          must_ext=["data-agcb-build-value=\"stairs\"","data-agcb-build-value=\"tile\"","data-agcb-build-value=\"ceramic\"","created.userData.shape='stairs'","created.userData.mat=STATE.material","upgradeStairs","schema:2"]
          must_mat=["kind==='tile'","kind==='ceramic'","MeshStandardMaterial","MATERIAL_SCHEMA=2"]
          must_collision=['geometryStamp','geometryInvalidations','cache.delete(object)']
          missing=[x for x in must_ext if x not in ext]+[x for x in must_mat if x not in mat]+[x for x in must_collision if x not in collision]
          if "import './building-extension-runtime.js';" not in boot: missing.append('bootstrap building extension import')
          if "building-extension-runtime.js" not in sw: missing.append('service worker cache entry')
          if "runtime26" not in sw: missing.append('runtime26 cache version')
          if missing:
            print('Missing building extension invariants:')
            print('\n'.join(missing));sys.exit(1)
          print('Stairs/tile/ceramic + collision invalidation invariants passed.')
